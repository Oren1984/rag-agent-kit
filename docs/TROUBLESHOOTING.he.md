# פתרון תקלות

## השירות לא עולה
- ודא שיש `.env` ושמוגדר `RAG_API_KEY`
- ודא שאתה מריץ מתוך שורש הריפו

## שגיאת 401
- שלח כותרת: `X-API-Key: <המפתח שלך>`
- ודא שהמפתח תואם ל־`.env`

## פורט תפוס
- שנה את המיפוי ב־`docker-compose.yml` או עצור שירות אחר
